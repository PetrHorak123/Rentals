@model Rentals.Web.Models.UserLoginViewModel
@using Rentals.Web.Localization

@{
	ViewBag.Title = Localization.Login;
	Layout = "~/Views/Shared/_EmptyLayout.cshtml";
}

<h1>
	Disclaimer: this is only for testing purposes, it's still missing history, employees administration, some kinky things with calendars, some views, notification and some minor tweeks, so it should be used only for testing purposes.
	<br />
	Login to administration is username:admin, password: P@ssw0rd
	<br />
	If some error occuser, exception page is visible, print screen it and sent to marek.honc@pslib.cz (hosting doesn't implement error sending, or i didn't find it)
	<br />
	Dont have Https, so office 365 doesnt work yet, I know, it's bad
</h1>

<form method="post" class="form-horizontal" asp-action="ExternalLogin" asp-controller="Account">
	@Html.HiddenFor(m => m.ReturnUrl)
	<div>
		<p>
			@if (Model.ExternalLogins != null)
			{
				foreach (var provider in Model.ExternalLogins)
				{
					<button type="submit" class="btn btn-default" name="@Html.NameFor(m => m.Provider)" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
				}
			}
		</p>
	</div>
</form>